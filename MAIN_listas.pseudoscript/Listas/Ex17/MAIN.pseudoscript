# Inverta a ordem dos elementos num array.

array = [1, 2, 3]
resultado = []

para(i = tamanho(array) -1, i > 0, i--)
	resultado.adicionar(array[i])

para(i = 0; i < tamanho(resultado); i++):
    imprime(resultado[i])


